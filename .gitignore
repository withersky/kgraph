# Python (кэши и окружения — проект без зависимостей, но на будущее)
__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/

# Локальная конфигурация Zed
# (.zed/settings.json содержит пути и разрешения конкретного автора)
.zed/

# Результаты `kgraph static` — <граф>.html рядом с исходным .jsonl
# (в репозитории исходников jsonl нет, но сгенерированные страницы коммитить не нужно)
*.html

# Системное
.DS_Store
Thumbs.db
